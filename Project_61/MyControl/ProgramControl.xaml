<UserControl x:Class="Project_61.MyControl.ProgramControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_61.MyControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Path=Variables.WorkingTime, StringFormat={}{0:hh':'mm':'ss}}" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="{Binding Path=Variables.FullName}" VerticalAlignment="Center"/>
        <CheckBox Grid.Column="2" IsChecked="{Binding Path=Variables.ParentalControl}" VerticalAlignment="Center"/>
        <ComboBox Grid.Column="3" ItemsSource="{Binding Path=WorkingTime}" SelectedItem="{Binding Path=Variables.SelectedWorkingTime}" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
