<UserControl x:Class="Project_61.MyControl.ProgramControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_61.MyControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Path=Variables.StartTime}" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="{Binding Path=Variables.FullName}" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="2" Text="{Binding Path=Variables.WorkTime}" VerticalAlignment="Center"/>
        <Label Grid.Column="3" Content="Отключить через:" VerticalAlignment="Center"/>
        <TextBox Grid.Column="4" Text="{Binding Path=Variables.TimeRun}" TextChanged="TextBox_TextChanged" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
